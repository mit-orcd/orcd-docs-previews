import sys              # Import system package 
n = int(sys.argv[1])    # Use the first input argument from command line
m = n - 1               # do some computation
print(n, m)             # Print the result 
